import React from 'react'

// import { toPersianNumber } from '../../../utils/translateNumber';
const UserInfo = (/*{user}*/) => {
    return (
        <>
        </>
    );
};

export default UserInfo;